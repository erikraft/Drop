name: Crowdin Translations Sync

permissions:
  contents: write

on:
  workflow_dispatch:    # permite rodar manualmente
  schedule:
    - cron: "0 */6 * * *"   # opcional: roda a cada 6h

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.BOT_GITHUB_TOKEN }}

      - name: Configure Git identity (BOT)
        run: |
          git config user.name "ErikrafT Drop Crowdin Bot"
          git config user.email "erikraft-drop-crowdin-bot@users.noreply.github.com"

      - name: Download translations from Crowdin
        uses: crowdin/github-action@v2
        with:
          command: download
          crowdin_project_id: ${{ secrets.CROWDIN_PROJECT_ID }}
          crowdin_personal_token: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}

      - name: Commit translations
        run: |
          git add .
          git diff --cached --quiet || git commit -m "chore(i18n): update translations from Crowdin"

      - name: Push changes
        run: git push
